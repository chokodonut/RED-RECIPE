<div class="container">
  <% @recipe.each do |recipe| %>
    <%= recipe.title %>
    <%= link_to recipe_path(recipe.id) do %>
      <%= image_tag recipe.get_image(150,150) %>
    <% end %>
    <%= recipe.description %>
  <% end %>
</div>