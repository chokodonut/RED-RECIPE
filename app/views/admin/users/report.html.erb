<div class="container">
  <h3><%= @user.name %>さんのつくれぽ</h3>
  <table class="table table-striped table-hover">
    <% if @report.present? %>
      <% @report.each do |report| %>
        <tr>
          <td>
            <%= image_tag report.get_image(100,100) %>
            <%= report.content %>
          </td>
  
          <td>
            <%= link_to "削除", admin_report_path(report.id), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-outline-danger" %>
          </td>
        </tr>
      <% end %>
    <% else %>
      <p>まだつくれぽを投稿していません。</p>
    <% end %>  
  </table>
</div>