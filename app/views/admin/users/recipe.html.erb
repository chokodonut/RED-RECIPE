<div class="container">
  <h3><%= @user.name %>さんのレシピ一覧</h3>
  <% if @recipe.present? %>
    <% @recipe.each do |recipe| %>
    <table class="table table-striped table-hover">
      <tr>
        <th style="width: 50%"></th>
        <th style="width: 20%"></th>
      </tr>
      <tr>
        <td>
          <%= link_to admin_recipe_path(recipe.id) do %>
            <%= image_tag recipe.get_image(200,200) %>
          <% end %>
          <%= recipe.title %>
        </td>
        <td>
          <%= link_to "削除", admin_recipe_path(recipe.id), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-outline-danger" %>
        </td>
      </tr>
    </table>
    <% end %>
  <% else %>
    <p>まだレシピを投稿していません。</p>
  <% end %>
</div>