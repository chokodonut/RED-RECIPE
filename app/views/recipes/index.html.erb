<div class="container">
  <h3>レシピ一覧</h3>
  <div class="row">
    <% @recipe.each do |recipe| %>
    <%= recipe.title %>
    <%= image_tag recipe.get_image(150,150) %>
    <%= recipe.description %>
    <% end %>
  </div>
</div>